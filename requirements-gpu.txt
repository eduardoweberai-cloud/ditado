# GPU NVIDIA (opcional; Windows e Linux). Sem isso o app roda em CPU.
# O instalador aplica automaticamente quando detecta nvidia-smi.
nvidia-cublas-cu12==12.9.2.10; sys_platform != "darwin"
nvidia-cudnn-cu12==9.24.0.43; sys_platform != "darwin"
